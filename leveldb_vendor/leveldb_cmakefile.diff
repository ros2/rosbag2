diff -Nur a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt	2019-05-04 04:24:26.000000000 +0800
+++ b/CMakeLists.txt	2020-11-10 18:44:46.835995753 +0800
@@ -27,6 +27,7 @@
 option(LEVELDB_BUILD_TESTS "Build LevelDB's unit tests" ON)
 option(LEVELDB_BUILD_BENCHMARKS "Build LevelDB's benchmarks" ON)
 option(LEVELDB_INSTALL "Install LevelDB's header and library" ON)
+option(SNAPPY_INSTALL_PATH "Install path for header and library of snappy" "")
 
 include(TestBigEndian)
 test_big_endian(LEVELDB_IS_BIG_ENDIAN)
@@ -36,7 +37,7 @@
 
 include(CheckLibraryExists)
 check_library_exists(crc32c crc32c_value "" HAVE_CRC32C)
-check_library_exists(snappy snappy_compress "" HAVE_SNAPPY)
+check_library_exists(snappy snappy_compress ${SNAPPY_INSTALL_PATH}/lib HAVE_SNAPPY)
 check_library_exists(tcmalloc malloc "" HAVE_TCMALLOC)
 
 include(CheckCXXSymbolExists)
@@ -94,6 +95,9 @@
   add_compile_options(-fvisibility=hidden)
 endif(BUILD_SHARED_LIBS)
 
+# Must be included before CMAKE_INSTALL_INCLUDEDIR is used.
+include(GNUInstallDirs)
+
 add_library(leveldb "")
 target_sources(leveldb
   PRIVATE
@@ -212,6 +216,7 @@
   PUBLIC
     $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include>
     $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>
+    ${SNAPPY_INSTALL_PATH}/include
 )
 
 set_target_properties(leveldb
@@ -249,6 +254,8 @@
   target_link_libraries(leveldb crc32c)
 endif(HAVE_CRC32C)
 if(HAVE_SNAPPY)
+  target_link_directories(leveldb
+												PUBLIC ${SNAPPY_INSTALL_PATH}/lib)
   target_link_libraries(leveldb snappy)
 endif(HAVE_SNAPPY)
 if(HAVE_TCMALLOC)
